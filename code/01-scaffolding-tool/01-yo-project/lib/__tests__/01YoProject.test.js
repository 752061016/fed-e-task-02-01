const assert = require('assert');
const 01YoProject = require('../index.js');

describe('01YoProject', () => {
  it('has a test', () => {
    assert(false, '01YoProject should have a test');
  });
});
